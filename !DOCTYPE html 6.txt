<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>游戏黑话 — Китайский геймерский сленг 2025-2026</title>
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+SC:wght@400;500;700;900&family=Inter:wght@400;600;800&display=swap" rel="stylesheet">
    <style>
        :root {
            --neon-blue: #00F0FF;
            --neon-pink: #FF2E63;
            --neon-purple: #B829DD;
            --neon-green: #39FF14;
            --dark-bg: #0A0A0F;
            --card-bg: #12121A;
            --text-primary: #FFFFFF;
            --text-secondary: #8B8B9E;
            --accent-gaming: #FF6B35;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
            background: var(--dark-bg);
            color: var(--text-primary);
            overflow-x: hidden;
            line-height: 1.6;
        }

        /* Animated Background */
        .bg-grid {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: 
                linear-gradient(rgba(0, 240, 255, 0.03) 1px, transparent 1px),
                linear-gradient(90deg, rgba(0, 240, 255, 0.03) 1px, transparent 1px);
            background-size: 50px 50px;
            z-index: 0;
            pointer-events: none;
        }

        .bg-glow {
            position: fixed;
            width: 600px;
            height: 600px;
            border-radius: 50%;
            filter: blur(100px);
            opacity: 0.15;
            z-index: 0;
            pointer-events: none;
        }

        .glow-1 { background: var(--neon-blue); top: -200px; left: -200px; }
        .glow-2 { background: var(--neon-purple); bottom: -200px; right: -200px; }

        /* Header */
        header {
            position: sticky;
            top: 0;
            background: rgba(10, 10, 15, 0.9);
            backdrop-filter: blur(20px);
            border-bottom: 1px solid rgba(0, 240, 255, 0.2);
            z-index: 1000;
            padding: 1rem 0;
        }

        .header-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 1.5rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .logo {
            display: flex;
            align-items: center;
            gap: 0.75rem;
            font-family: 'Noto Sans SC', sans-serif;
            font-size: 1.5rem;
            font-weight: 900;
            color: var(--neon-blue);
            text-decoration: none;
            text-shadow: 0 0 20px rgba(0, 240, 255, 0.5);
        }

        .logo-icon {
            width: 44px;
            height: 44px;
            background: linear-gradient(135deg, var(--neon-blue), var(--neon-purple));
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.5rem;
            box-shadow: 0 0 20px rgba(0, 240, 255, 0.3);
            animation: pulse 2s infinite;
        }

        @keyframes pulse {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.05); }
        }

        .level-badge {
            background: linear-gradient(135deg, var(--neon-pink), var(--accent-gaming));
            color: white;
            padding: 0.5rem 1rem;
            border-radius: 20px;
            font-size: 0.85rem;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.05em;
        }

        /* Hero Section */
        .hero {
            position: relative;
            padding: 4rem 1.5rem 3rem;
            text-align: center;
            z-index: 1;
        }

        .hero-badge {
            display: inline-block;
            background: rgba(0, 240, 255, 0.1);
            border: 1px solid var(--neon-blue);
            padding: 0.5rem 1.5rem;
            border-radius: 50px;
            color: var(--neon-blue);
            font-weight: 600;
            font-size: 0.9rem;
            margin-bottom: 1.5rem;
            text-transform: uppercase;
            letter-spacing: 0.1em;
        }

        h1 {
            font-family: 'Noto Sans SC', sans-serif;
            font-size: clamp(2rem, 6vw, 3.5rem);
            font-weight: 900;
            background: linear-gradient(135deg, var(--neon-blue), var(--neon-pink));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            margin-bottom: 1rem;
            line-height: 1.2;
        }

        .hero-subtitle {
            font-size: 1.1rem;
            color: var(--text-secondary);
            max-width: 600px;
            margin: 0 auto 2rem;
        }

        .hero-stats {
            display: flex;
            justify-content: center;
            gap: 2rem;
            flex-wrap: wrap;
            margin-top: 2rem;
        }

        .stat-box {
            background: var(--card-bg);
            border: 1px solid rgba(0, 240, 255, 0.2);
            padding: 1rem 1.5rem;
            border-radius: 16px;
            text-align: center;
        }

        .stat-value {
            font-size: 1.5rem;
            font-weight: 800;
            color: var(--neon-green);
        }

        .stat-label {
            font-size: 0.8rem;
            color: var(--text-secondary);
            text-transform: uppercase;
        }

        /* Navigation Tabs */
        .nav-tabs {
            display: flex;
            gap: 0.5rem;
            overflow-x: auto;
            padding: 0 1.5rem 1rem;
            max-width: 1200px;
            margin: 0 auto;
            scrollbar-width: none;
        }

        .nav-tabs::-webkit-scrollbar { display: none; }

        .nav-tab {
            flex-shrink: 0;
            padding: 0.75rem 1.25rem;
            background: var(--card-bg);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 50px;
            color: var(--text-secondary);
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s;
            white-space: nowrap;
        }

        .nav-tab:hover {
            border-color: var(--neon-blue);
            color: var(--neon-blue);
        }

        .nav-tab.active {
            background: linear-gradient(135deg, var(--neon-blue), var(--neon-purple));
            color: white;
            border-color: transparent;
            box-shadow: 0 4px 20px rgba(0, 240, 255, 0.3);
        }

        /* Sections */
        section {
            position: relative;
            z-index: 1;
            padding: 3rem 1.5rem;
            max-width: 1200px;
            margin: 0 auto;
            display: none;
        }

        section.active {
            display: block;
            animation: fadeIn 0.5s ease;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .section-title {
            font-family: 'Noto Sans SC', sans-serif;
            font-size: 1.8rem;
            font-weight: 900;
            text-align: center;
            margin-bottom: 2rem;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.5rem;
            flex-wrap: wrap;
        }

        .game-tag {
            background: rgba(255, 107, 53, 0.2);
            color: var(--accent-gaming);
            padding: 0.25rem 0.75rem;
            border-radius: 20px;
            font-size: 0.75rem;
            font-weight: 700;
            text-transform: uppercase;
        }

        /* Cards Grid */
        .cards-grid {
            display: grid;
            grid-template-columns: repeat(1, 1fr);
            gap: 1rem;
        }

        @media (min-width: 640px) {
            .cards-grid { grid-template-columns: repeat(2, 1fr); }
        }

        @media (min-width: 1024px) {
            .cards-grid { grid-template-columns: repeat(3, 1fr); }
        }

        .slang-card {
            background: var(--card-bg);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 20px;
            padding: 1.5rem;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        .slang-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 3px;
            background: linear-gradient(90deg, var(--neon-blue), var(--neon-purple));
            transform: scaleX(0);
            transition: transform 0.3s;
        }

        .slang-card:hover {
            transform: translateY(-5px);
            border-color: var(--neon-blue);
            box-shadow: 0 10px 40px rgba(0, 240, 255, 0.1);
        }

        .slang-card:hover::before {
            transform: scaleX(1);
        }

        .card-header {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            margin-bottom: 1rem;
        }

        .slang-term {
            font-family: 'Noto Sans SC', sans-serif;
            font-size: 2rem;
            font-weight: 900;
            color: var(--text-primary);
            line-height: 1;
        }

        .slang-pinyin {
            color: var(--neon-blue);
            font-size: 1rem;
            font-weight: 600;
            margin-top: 0.25rem;
            letter-spacing: 0.05em;
        }

        .tier-badge {
            padding: 0.25rem 0.5rem;
            border-radius: 8px;
            font-size: 0.7rem;
            font-weight: 700;
            text-transform: uppercase;
        }

        .tier-s { background: linear-gradient(135deg, #FFD700, #FFA500); color: #000; }
        .tier-a { background: linear-gradient(135deg, #C0C0C0, #808080); color: #000; }
        .tier-b { background: linear-gradient(135deg, #CD7F32, #8B4513); color: #fff; }

        .slang-meaning {
            color: var(--text-secondary);
            font-size: 0.95rem;
            margin-bottom: 1rem;
            line-height: 1.5;
        }

        .origin-tag {
            display: inline-block;
            background: rgba(184, 41, 221, 0.2);
            color: var(--neon-purple);
            padding: 0.25rem 0.75rem;
            border-radius: 20px;
            font-size: 0.75rem;
            margin-bottom: 0.75rem;
        }

        .example-box {
            background: rgba(0, 240, 255, 0.05);
            border-left: 3px solid var(--neon-blue);
            padding: 1rem;
            border-radius: 0 12px 12px 0;
            margin-top: 0.75rem;
        }

        .example-cn {
            font-family: 'Noto Sans SC', sans-serif;
            font-size: 1.1rem;
            color: var(--text-primary);
            margin-bottom: 0.25rem;
        }

        .example-ru {
            color: var(--text-secondary);
            font-size: 0.9rem;
            font-style: italic;
        }

        /* Abbreviations Section */
        .abbr-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 1rem;
        }

        @media (min-width: 640px) {
            .abbr-grid { grid-template-columns: repeat(3, 1fr); }
        }

        @media (min-width: 1024px) {
            .abbr-grid { grid-template-columns: repeat(4, 1fr); }
        }

        .abbr-card {
            background: var(--card-bg);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 16px;
            padding: 1.25rem;
            text-align: center;
            transition: all 0.3s;
        }

        .abbr-card:hover {
            border-color: var(--neon-green);
            transform: scale(1.02);
        }

        .abbr-code {
            font-family: 'Noto Sans SC', sans-serif;
            font-size: 1.5rem;
            font-weight: 900;
            color: var(--neon-green);
            margin-bottom: 0.5rem;
        }

        .abbr-full {
            font-size: 0.9rem;
            color: var(--text-secondary);
            margin-bottom: 0.5rem;
        }

        .abbr-meaning {
            font-size: 0.85rem;
            color: var(--text-primary);
        }

        /* Quiz Section */
        .quiz-container {
            background: var(--card-bg);
            border: 1px solid rgba(0, 240, 255, 0.3);
            border-radius: 24px;
            padding: 2rem;
            max-width: 800px;
            margin: 0 auto;
        }

        .quiz-progress {
            background: rgba(255, 255, 255, 0.1);
            height: 8px;
            border-radius: 4px;
            margin-bottom: 2rem;
            overflow: hidden;
        }

        .quiz-progress-bar {
            background: linear-gradient(90deg, var(--neon-blue), var(--neon-green));
            height: 100%;
            width: 0%;
            transition: width 0.3s;
        }

        .quiz-question {
            text-align: center;
            margin-bottom: 2rem;
        }

        .quiz-term {
            font-family: 'Noto Sans SC', sans-serif;
            font-size: 3rem;
            font-weight: 900;
            color: var(--neon-blue);
            margin-bottom: 1rem;
        }

        .quiz-options {
            display: grid;
            gap: 1rem;
        }

        .quiz-option {
            background: rgba(255, 255, 255, 0.05);
            border: 2px solid rgba(255, 255, 255, 0.1);
            padding: 1.25rem;
            border-radius: 16px;
            cursor: pointer;
            transition: all 0.2s;
            text-align: left;
            font-size: 1rem;
        }

        .quiz-option:hover {
            border-color: var(--neon-blue);
            background: rgba(0, 240, 255, 0.05);
            transform: translateX(5px);
        }

        .quiz-option.correct {
            background: rgba(57, 255, 20, 0.2);
            border-color: var(--neon-green);
            color: var(--neon-green);
        }

        .quiz-option.wrong {
            background: rgba(255, 46, 99, 0.2);
            border-color: var(--neon-pink);
            color: var(--neon-pink);
            animation: shake 0.5s;
        }

        @keyframes shake {
            0%, 100% { transform: translateX(0); }
            25% { transform: translateX(-10px); }
            75% { transform: translateX(10px); }
        }

        .quiz-result {
            text-align: center;
            padding: 2rem;
            display: none;
        }

        .quiz-result.show {
            display: block;
        }

        .result-rank {
            font-size: 4rem;
            margin-bottom: 1rem;
        }

        /* Meme Generator */
        .meme-section {
            text-align: center;
        }

        .meme-display {
            background: var(--card-bg);
            border: 2px dashed var(--neon-purple);
            border-radius: 24px;
            padding: 3rem 2rem;
            margin-top: 2rem;
            min-height: 300px;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            transition: all 0.3s;
        }

        .meme-display.active {
            border-style: solid;
            border-color: var(--neon-blue);
            background: linear-gradient(135deg, var(--card-bg), rgba(0, 240, 255, 0.05));
        }

        .meme-term {
            font-family: 'Noto Sans SC', sans-serif;
            font-size: 3.5rem;
            font-weight: 900;
            color: var(--neon-blue);
            margin-bottom: 0.5rem;
        }

        .meme-pinyin {
            color: var(--neon-pink);
            font-size: 1.3rem;
            font-weight: 600;
            margin-bottom: 1rem;
        }

        .meme-meaning {
            font-size: 1.2rem;
            color: var(--text-primary);
            margin-bottom: 1rem;
            max-width: 500px;
        }

        .meme-origin {
            background: rgba(184, 41, 221, 0.2);
            color: var(--neon-purple);
            padding: 0.5rem 1rem;
            border-radius: 20px;
            font-size: 0.85rem;
            margin-bottom: 1.5rem;
        }

        /* Footer */
        footer {
            background: var(--card-bg);
            border-top: 1px solid rgba(0, 240, 255, 0.2);
            padding: 3rem 1.5rem 2rem;
            text-align: center;
            margin-top: 4rem;
            position: relative;
            z-index: 1;
        }

        .footer-slogan {
            font-family: 'Noto Sans SC', sans-serif;
            font-size: 1.5rem;
            background: linear-gradient(135deg, var(--neon-blue), var(--neon-pink));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            margin-bottom: 1rem;
        }

        .btn-primary {
            background: linear-gradient(135deg, var(--neon-blue), var(--neon-purple));
            color: white;
            border: none;
            padding: 1rem 2rem;
            border-radius: 50px;
            font-weight: 700;
            font-size: 1.1rem;
            cursor: pointer;
            transition: all 0.3s;
            box-shadow: 0 4px 20px rgba(0, 240, 255, 0.3);
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
        }

        .btn-primary:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 30px rgba(0, 240, 255, 0.4);
        }

        .btn-primary:active {
            transform: scale(0.98);
        }

        /* Scroll Animations */
        .fade-in {
            opacity: 0;
            transform: translateY(30px);
            transition: all 0.6s ease;
        }

        .fade-in.visible {
            opacity: 1;
            transform: translateY(0);
        }

        /* Mobile Optimizations */
        @media (max-width: 480px) {
            .hero { padding: 3rem 1rem 2rem; }
            .slang-term { font-size: 1.5rem; }
            .quiz-term { font-size: 2rem; }
            .meme-term { font-size: 2.5rem; }
            .section-title { font-size: 1.4rem; }
        }

        /* Comparison Cards */
        .compare-grid {
            display: grid;
            grid-template-columns: 1fr;
            gap: 1.5rem;
        }

        @media (min-width: 768px) {
            .compare-grid { grid-template-columns: repeat(2, 1fr); }
        }

        .compare-card {
            background: var(--card-bg);
            border-radius: 20px;
            padding: 1.5rem;
            border: 1px solid rgba(255, 255, 255, 0.1);
        }

        .compare-pair {
            display: flex;
            gap: 1rem;
            margin-bottom: 1rem;
            align-items: center;
        }

        .compare-item {
            flex: 1;
            background: rgba(255, 255, 255, 0.05);
            padding: 1rem;
            border-radius: 12px;
            text-align: center;
        }

        .vs-divider {
            color: var(--neon-pink);
            font-weight: 800;
            font-size: 0.9rem;
        }

        .compare-note {
            background: rgba(0, 240, 255, 0.1);
            padding: 0.75rem;
            border-radius: 8px;
            font-size: 0.9rem;
            color: var(--text-secondary);
            border-left: 3px solid var(--neon-blue);
        }
    </style>
</head>
<body>
    <div class="bg-grid"></div>
    <div class="bg-glow glow-1"></div>
    <div class="bg-glow glow-2"></div>

    <!-- Header -->
    <header>
        <div class="header-container">
            <a href="#" class="logo">
                <div class="logo-icon">🎮</div>
                <span>游戏黑话</span>
            </a>
            <div class="level-badge">LV.2026</div>
        </div>
    </header>

    <!-- Hero -->
    <section class="hero active" id="heroSection">
        <div class="hero-badge">🔥 2025-2026 Gaming Slang</div>
        <h1>别做菜鸟</h1>
        <p class="hero-subtitle">
            Китайский геймерский сленг из MOBA, shooters, RPG и стримов.<br>
            От «666» до「破防了」— говори как про-геймер с Douyu и Bilibili.
        </p>
        <button class="btn-primary" onclick="showSection('basics')">
            <span>🚀</span> Начать обучение
        </button>
        <div class="hero-stats">
            <div class="stat-box">
                <div class="stat-value">50+</div>
                <div class="stat-label">Сленг</div>
            </div>
            <div class="stat-box">
                <div class="stat-value">10+</div>
                <div class="stat-label">Игр</div>
            </div>
            <div class="stat-box">
                <div class="stat-value">2026</div>
                <div class="stat-label">Актуально</div>
            </div>
        </div>
    </section>

    <!-- Navigation -->
    <div class="nav-tabs" id="navTabs" style="display: none;">
        <button class="nav-tab active" onclick="showSection('basics')">Основы</button>
        <button class="nav-tab" onclick="showSection('moba')">MOBA</button>
        <button class="nav-tab" onclick="showSection('shooters')">Шутеры</button>
        <button class="nav-tab" onclick="showSection('abbreviations')">Аббревиатуры</button>
        <button class="nav-tab" onclick="showSection('memes')">Мемы 2025</button>
        <button class="nav-tab" onclick="showSection('confusing')">Не путай</button>
        <button class="nav-tab" onclick="showSection('quiz')">Тест</button>
        <button class="nav-tab" onclick="showSection('random')">Рандом</button>
    </div>

    <!-- Basics Section -->
    <section id="basics">
        <h2 class="section-title">
            <span class="game-tag">BASIC</span>
            Базовый сленг
        </h2>
        <div class="cards-grid" id="basicsGrid"></div>
    </section>

    <!-- MOBA Section -->
    <section id="moba">
        <h2 class="section-title">
            <span class="game-tag">MOBA</span>
            League of Legends / Honor of Kings / DOTA2
        </h2>
        <div class="cards-grid" id="mobaGrid"></div>
    </section>

    <!-- Shooters Section -->
    <section id="shooters">
        <h2 class="section-title">
            <span class="game-tag">FPS</span>
            Valorant / CS2 / PUBG / Delta Force
        </h2>
        <div class="cards-grid" id="shootersGrid"></div>
    </section>

    <!-- Abbreviations Section -->
    <section id="abbreviations">
        <h2 class="section-title">
            <span class="game-tag">CHAT</span>
            Аббревиатуры и сокращения
        </h2>
        <div class="abbr-grid" id="abbrGrid"></div>
    </section>

    <!-- Memes 2025 Section -->
    <section id="memes">
        <h2 class="section-title">
            <span class="game-tag">2025</span>
            Горячие мемы года
        </h2>
        <div class="cards-grid" id="memesGrid"></div>
    </section>

    <!-- Confusing Section -->
    <section id="confusing">
        <h2 class="section-title">
            <span class="game-tag">WARNING</span>
            ⚠️ Легко спутать
        </h2>
        <div class="compare-grid" id="confusingGrid"></div>
    </section>

    <!-- Quiz Section -->
    <section id="quiz">
        <div class="quiz-container">
            <h2 class="section-title">🎮 Проверь свой скилл</h2>
            <div class="quiz-progress">
                <div class="quiz-progress-bar" id="quizProgress"></div>
            </div>
            <div id="quizContent"></div>
            <div class="quiz-result" id="quizResult">
                <div class="result-rank" id="resultRank"></div>
                <h3 id="resultTitle"></h3>
                <p id="resultText"></p>
                <button class="btn-primary" onclick="restartQuiz()" style="margin-top: 1rem;">
                    Переиграть
                </button>
            </div>
        </div>
    </section>

    <!-- Random Meme Section -->
    <section id="random" class="meme-section">
        <h2 class="section-title">🎲 Случайный мем</h2>
        <button class="btn-primary" onclick="generateRandomMeme()" style="font-size: 1.2rem;">
            <span>🎰</span> Роллить
        </button>
        <div class="meme-display" id="memeDisplay">
            <p style="color: var(--text-secondary); font-size: 1.1rem;">
                Нажми кнопку выше для случайного геймерского термина!
            </p>
        </div>
    </section>

    <!-- Footer -->
    <footer>
        <div class="footer-slogan">GG WP 下次见</div>
        <p style="color: var(--text-secondary); font-size: 0.9rem; margin-bottom: 1rem;">
            Made for gamers who want to understand Chinese teammates
        </p>
        <button class="btn-primary" onclick="showSection('heroSection')">
            <span>🏠</span> В начало
        </button>
    </footer>

    <script>
        // Data
        const basicsData = [
            {
                term: '666',
                pinyin: 'liù liù liù',
                meaning: 'Нереально круто / wp (well played)',
                tier: 's',
                origin: 'Гейминг / Стримы',
                example: '这波操作666！',
                translation: 'Этот мув просто 666! (огонь)',
                desc: 'Число 6 (六 liù) созвучно со словом "溜 liū" (скользить/круто). Три шестёрки = максимальный респект.'
            },
            {
                term: '菜鸟',
                pinyin: 'cài niǎo',
                meaning: 'Нуб / рак / новичок',
                tier: 'a',
                origin: 'Общий сленг',
                example: '我就是个菜鸟',
                translation: 'Я просто нуб',
                desc: 'Дословно "овощная птица". 菜 cài = овощ/плохой игрок, 鸟 niǎo = птица (просто дополнение).'
            },
            {
                term: '大神',
                pinyin: 'dà shén',
                meaning: 'Бог игры / гуру / керри',
                tier: 's',
                origin: 'Общий сленг',
                example: '求大神带飞',
                translation: 'Боже, тащи меня',
                desc: 'Дословно "великий бог". Обращение к сильному игроку с просьбой помочь.'
            },
            {
                term: '开黑',
                pinyin: 'kāi hēi',
                meaning: 'Играть пати / стаком',
                tier: 'a',
                origin: 'MOBA',
                example: '五黑吗？',
                translation: 'Пати из 5 человек?',
                desc: 'Играть вместе с друзьями в голосовом чате. 黑 hēi = тёмный (подпольный) чат.'
            },
            {
                term: '坑',
                pinyin: 'kēng',
                meaning: 'Тиммейт-рак / фидер',
                tier: 'a',
                origin: 'MOBA',
                example: '别坑我啊！',
                translation: 'Не фиди/не сливай!',
                desc: 'Дословно "яма". Игрок, который тянет команду вниз, как в яму.'
            },
            {
                term: '躺赢',
                pinyin: 'tǎng yíng',
                meaning: 'Выиграть лёжа / быть протащенным',
                tier: 'b',
                origin: 'MOBA',
                example: '这把躺赢了',
                translation: 'Эту катку выиграли лёжа',
                desc: 'Когда сильные тиммейты делают всё за тебя, а ты просто существуешь.'
            },
            {
                term: '破防了',
                pinyin: 'pò fáng le',
                meaning: 'Сломался / эмоциональный срыв',
                tier: 's',
                origin: 'MOBA → Общий',
                example: '这波操作让我破防了',
                translation: 'Этот мув сломал мне психику',
                desc: 'Изначально "пробить защиту" (armor break). Сейчас = эмоциональная пробитость [^16^].'
            },
            {
                term: '硬控',
                pinyin: 'yìng kòng',
                meaning: 'Хард-контроль / заставить замереть',
                tier: 'a',
                origin: 'MOBA → TikTok',
                example: '被这视频硬控30秒',
                translation: 'Это видео заставило меня зависнуть на 30 сек',
                desc: 'Изначально hard CC (crowd control). Сейчас используется для всего, что "парализует" внимание [^12^].'
            }
        ];

        const mobaData = [
            {
                term: '送人头',
                pinyin: 'sòng rén tóu',
                meaning: 'Фид / отдавать киллы врагу',
                tier: 's',
                origin: 'League of Legends',
                example: '别送人头了！',
                translation: 'Хватит фидить!',
                desc: 'Дословно "дарить головы". Постоянно умирать и кормить врага.'
            },
            {
                term: '打野',
                pinyin: 'dǎ yě',
                meaning: 'Джангл / фармить лес',
                tier: 'a',
                origin: 'DOTA2 / LoL',
                example: '打野来抓',
                translation: 'Джанглер идёт гэнкать',
                desc: '打 dǎ = бить, 野 yě = дикий/лес. Игрок, который фармит нейтральных крипов.'
            },
            {
                term: '补兵',
                pinyin: 'bǔ bīng',
                meaning: 'Денай / добивать крипов',
                tier: 'a',
                origin: 'DOTA2',
                example: '补兵很重要',
                translation: 'Денай важен',
                desc: '补 bǔ = восполнять. Добивание своих крипов, чтобы враг не получил золото/опыт.'
            },
            {
                term: '大招',
                pinyin: 'dà zhāo',
                meaning: 'Ультимейт / ульта',
                tier: 's',
                origin: 'Все MOBA',
                example: '等我大招CD',
                translation: 'Жди кд моей ульты',
                desc: '大 dà = большой, 招 zhāo = приём/навык. Главный скилл персонажа.'
            },
            {
                term: '开团',
                pinyin: 'kāi tuán',
                meaning: 'Начать файт / инициировать',
                tier: 'a',
                origin: 'MOBA',
                example: '准备开团',
                translation: 'Готовьтесь к файту',
                desc: '开 kāi = открывать/начинать, 团 tuán = команда. Начало командного боя.'
            },
            {
                term: 'GG',
                pinyin: 'GG',
                meaning: 'GG (Good Game)',
                tier: 's',
                origin: 'Все игры',
                example: 'GG，投了吧',
                translation: 'ГГ, сдавайтесь',
                desc: 'Заимствовано из английского. Может означать как "хорошая игра", так и "всё, проиграли".'
            }
        ];

        const shootersData = [
            {
                term: '颗秒',
                pinyin: 'kē miǎo',
                meaning: 'Ваншот / one-tap',
                tier: 'a',
                origin: 'Valorant / CS2',
                example: '他被颗秒了',
                translation: 'Его ваншотнули',
                desc: '颗 kē = пуля/гранула. Убить одной пулей в голову.'
            },
            {
                term: '马枪',
                pinyin: 'mǎ qiāng',
                meaning: 'Промахнуться / спрей в молоко',
                tier: 'b',
                origin: 'CS / Valorant',
                example: '我马枪了',
                translation: 'Я промазал',
                desc: '马 mǎ = лошадь (тут: промах). Когда стреляешь много, но не попадаешь.'
            },
            {
                term: '老六',
                pinyin: 'lǎo liù',
                meaning: 'Кемпер / сидящий в углу',
                tier: 's',
                origin: 'PUBG / CS',
                example: '这有个老六',
                translation: 'Тут кемпер сидит',
                desc: '6-й игрок в команде из 5 человек = лишний, сидящий в засаде. Сейчас = любой кемпер.'
            },
            {
                term: '白给',
                pinyin: 'bái gěi',
                meaning: 'Отдать килл даром / фид',
                tier: 'a',
                origin: 'CS:GO',
                example: '又白给了',
                translation: 'Опять отдался',
                desc: '白 bái = зря/бесплатно. Умереть, не принеся пользы команде.'
            },
            {
                term: '瓦学弟',
                pinyin: 'wǎ xué dì',
                meaning: 'Игрок Valorant (шутл.)',
                tier: 'b',
                origin: 'Valorant',
                example: '瓦学弟叫妈妈',
                translation: 'Валорантёр зовёт маму',
                desc: '瓦 wǎ = сокращение от 瓦罗兰特 (Valorant). Шутливое прозвище, часто в контексте "мамкиных сынков" [^6^].'
            },
            {
                term: 'Go学长',
                pinyin: 'Go xué zhǎng',
                meaning: 'CS игрок (старший брат)',
                tier: 'b',
                origin: 'CS2 vs Valorant',
                example: 'Go学长又嘲讽瓦学弟',
                translation: 'CS-шник снова троллит валорантёра',
                desc: 'Соперничество между CS и Valorant сообществами. Go = CS:GO, 学长 = старшекурсник [^6^].'
            }
        ];

        const abbreviationsData = [
            { code: 'xswl', full: '笑死我了', meaning: 'LOL (умираю со смеха)' },
            { code: 'yyds', full: '永远的神', meaning: 'Легенда / GOAT' },
            { code: 'nsdd', full: '你说得对', meaning: 'Ты прав (саркастично)' },
            { code: 'awsl', full: '啊我死了', meaning: 'Омг я умер (от милоты/крутости)' },
            { code: 'bbl', full: '拜拜了', meaning: 'Пока / бай-бай' },
            { code: 'cdx', full: '处对象', meaning: 'Встречаться (романтика)' },
            { code: 'cqy', full: '处Q友', meaning: 'Дружить в QQ' },
            { code: 'dbq', full: '对不起', meaning: 'Извини' },
            { code: 'gkd', full: '搞快点', meaning: 'Быстрее / hurry up' },
            { code: 'pyq', full: '朋友圈', meaning: 'Лента WeChat' },
            { code: 'srds', full: '虽然但是', meaning: 'Хотя / nevertheless' },
            { code: 'u1s1', full: '有一说一', meaning: 'Честно говоря / tbh' }
        ];

        const memes2025Data = [
            {
                term: '阿玛特拉斯',
                pinyin: 'Ā mǎ tè lā sī',
                meaning: 'Аматэрасу (из Наруто)',
                tier: 's',
                origin: '火影手游 / Coke主播',
                example: '阿玛特拉斯！',
                translation: 'Аматэрасу! (закрывает глаз рукой)',
                desc: 'Мем от стримера Coke, который закрывает глаз рукой, произнося "Аматэрасу". Стал вирусным жестом [^1^][^6^].'
            },
            {
                term: '有的兄弟有的',
                pinyin: 'Yǒu de xiōngdì yǒu de',
                meaning: 'Есть, брат, есть',
                tier: 'a',
                origin: '王者荣耀 / 千年主播',
                example: '有的兄弟有的，这么强的英雄还有9个',
                translation: 'Есть, брат, есть — ещё 9 таких сильных героев',
                desc: 'Мемный ответ стримера на вопрос "Есть ли сильный герой?". Используется для преувеличения [^1^].'
            },
            {
                term: '老爸得了MVP',
                pinyin: 'Lǎobà dé le MVP',
                meaning: 'Папа получил MVP',
                tier: 'a',
                origin: '英雄联盟 / 小明剑魔',
                example: '我老爸得了MVP！',
                translation: 'Мой отец получил MVP!',
                desc: 'Мем от стримера 小明剑魔 про странную систему оценки в играх. Сатира на логику рейтингов [^1^][^6^].'
            },
            {
                term: '回答我',
                pinyin: 'Huídá wǒ',
                meaning: 'Ответь мне',
                tier: 's',
                origin: '小明剑魔 / B站',
                example: '回答我！Look in my eyes！',
                translation: 'Ответь мне! Смотри мне в глаза!',
                desc: 'Один из самых вирусных мемов 2025. Агрессивная форма "ответь, почему" [^1^][^6^].'
            },
            {
                term: '跪下',
                pinyin: 'Guì xià',
                meaning: 'На колени',
                tier: 'a',
                origin: 'LPL / Doinb',
                example: '跪下！',
                translation: 'На колени!',
                desc: 'Скандальная фраза про-игрока Doinb после победы над IG. Вызвала бурные споры в комьюнити [^1^].'
            },
            {
                term: '摸头闪避',
                pinyin: 'Mō tóu shǎnbì',
                meaning: 'Уклонение с поправкой волос',
                tier: 'b',
                origin: 'Unleash the Avatar (India)',
                example: '这游戏有摸头闪避',
                translation: 'В этой игре есть уклонение с причёсыванием',
                desc: 'Мем из индийской игры, где персонаж после уклонения медленно поправляет волосы. "Слишком круто для Миядзаки" [^6^].'
            }
        ];

        const confusingData = [
            {
                item1: { term: '666', pinyin: 'liù liù liù', meaning: 'Круто / молодец' },
                item2: { term: '555', pinyin: 'wǔ wǔ wǔ', meaning: 'Плачу / грустно' },
                note: '666 = огонь, 555 = плачу (5 звучит как "呜" — звук плача)'
            },
            {
                item1: { term: '大神', pinyin: 'dà shén', meaning: 'Бог игры (крутой)' },
                item2: { term: '大神', pinyin: 'dà shén', meaning: 'Божок (вредный)' },
                note: 'В зависимости от тона: уважение или сарказм. Контекст решает всё!'
            },
            {
                item1: { term: '开黑', pinyin: 'kāi hēi', meaning: 'Играть пати (друзья)' },
                item2: { term: '开挂', pinyin: 'kāi guà', meaning: 'Читерить (хаки)' },
                note: '黑 hēi = тёмный (пати), 挂 guà = висеть (чит-программа). Разница огромная!'
            },
            {
                item1: { term: '硬控', pinyin: 'yìng kòng', meaning: 'Хард-CC / заставить замереть' },
                item2: { term: '硬刚', pinyin: 'yìng gāng', meaning: 'Лицом к лицу / напрямую' },
                note: '控 = контроль, 刚 = твёрдый/напрямую. Не путай в MOBA!'
            }
        ];

        const quizQuestions = [
            {
                term: '666',
                question: 'Что значит "666" в китайском гейминге?',
                options: [
                    'Дьявол / сатана',
                    'Нереально круто / well played',
                    'Читер / хакер',
                    'Прощание / пока'
                ],
                correct: 1,
                explanation: '666 (liù liù liù) = 溜溜溜 (liū liū liū) = "скользит" = круто. Максимальный комплимент скиллу.'
            },
            {
                term: '破防了',
                question: 'Как перевести "破防了" (pò fáng le)?',
                options: [
                    'Сломал броню (в игре)',
                    'Эмоциональный срыв / "я сломался"',
                    'Взломал защиту',
                    'Пробил броню врага'
                ],
                correct: 1,
                explanation: 'Изначально из MOBA (пробить armor), но сейчас = эмоциональная пробитость, когда что-то слишком трогательное или бесячее [^16^].'
            },
            {
                term: '老六',
                question: 'Кто такой "老六" (lǎo liù)?',
                options: [
                    '6-й уровень / ранг',
                    'Кемпер / сидящий в углу',
                    'Шестой игрок в команде',
                    'Старший брат'
                ],
                correct: 1,
                explanation: 'Изначально 6-й лишний в команде из 5. Сейчас = любой кемпер, который сидит в засаде и не играет с командой.'
            },
            {
                term: '硬控',
                question: 'Что такое "硬控" (yìng kòng)?',
                options: [
                    'Жёсткий контроллер',
                    'Хард-CC / заставить замереть',
                    'Сложный босс',
                    'Тяжёлый удар'
                ],
                correct: 1,
                explanation: 'Из MOBA (hard crowd control). Сейчас используется шире — когда что-то "парализует" твоё внимание [^12^].'
            },
            {
                term: '躺赢',
                question: 'Что значит "躺赢" (tǎng yíng)?',
                options: [
                    'Лежать и выигрывать (быть протащенным)',
                    'Выиграть лёжа (афк)',
                    'Лёгкая победа',
                    'Всё вышеперечисленное'
                ],
                correct: 3,
                explanation: '躺赢 = лежать + выигрывать. Когда сильные тиммейты тащат, а ты просто существуешь в катке.'
            }
        ];

        // State
        let currentQuiz = 0;
        let quizScore = 0;
        let allTerms = [];

        // Init
        document.addEventListener('DOMContentLoaded', () => {
            // Combine all terms for random generator
            allTerms = [...basicsData, ...mobaData, ...shootersData, ...memes2025Data];
            
            renderBasics();
            renderMoba();
            renderShooters();
            renderAbbreviations();
            renderMemes();
            renderConfusing();
            initQuiz();
            
            // Show nav after hero
            setTimeout(() => {
                document.getElementById('navTabs').style.display = 'flex';
            }, 100);
        });

        // Render functions
        function renderBasics() {
            const grid = document.getElementById('basicsGrid');
            grid.innerHTML = basicsData.map(item => createCard(item)).join('');
        }

        function renderMoba() {
            const grid = document.getElementById('mobaGrid');
            grid.innerHTML = mobaData.map(item => createCard(item)).join('');
        }

        function renderShooters() {
            const grid = document.getElementById('shootersGrid');
            grid.innerHTML = shootersData.map(item => createCard(item)).join('');
        }

        function renderAbbreviations() {
            const grid = document.getElementById('abbrGrid');
            grid.innerHTML = abbreviationsData.map(item => `
                <div class="abbr-card fade-in">
                    <div class="abbr-code">${item.code}</div>
                    <div class="abbr-full">${item.full}</div>
                    <div class="abbr-meaning">${item.meaning}</div>
                </div>
            `).join('');
        }

        function renderMemes() {
            const grid = document.getElementById('memesGrid');
            grid.innerHTML = memes2025Data.map(item => createCard(item)).join('');
        }

        function renderConfusing() {
            const grid = document.getElementById('confusingGrid');
            grid.innerHTML = confusingData.map(item => `
                <div class="compare-card fade-in">
                    <div class="compare-pair">
                        <div class="compare-item">
                            <div style="font-family: 'Noto Sans SC'; font-size: 1.5rem; font-weight: 900;">${item.item1.term}</div>
                            <div style="color: var(--neon-blue); font-size: 0.9rem;">${item.item1.pinyin}</div>
                            <div style="font-size: 0.85rem; color: var(--text-secondary); margin-top: 0.5rem;">${item.item1.meaning}</div>
                        </div>
                        <div class="vs-divider">VS</div>
                        <div class="compare-item">
                            <div style="font-family: 'Noto Sans SC'; font-size: 1.5rem; font-weight: 900;">${item.item2.term}</div>
                            <div style="color: var(--neon-pink); font-size: 0.9rem;">${item.item2.pinyin}</div>
                            <div style="font-size: 0.85rem; color: var(--text-secondary); margin-top: 0.5rem;">${item.item2.meaning}</div>
                        </div>
                    </div>
                    <div class="compare-note">${item.note}</div>
                </div>
            `).join('');
        }

        function createCard(item) {
            const tierClass = `tier-${item.tier}`;
            return `
                <div class="slang-card fade-in">
                    <div class="card-header">
                        <div>
                            <div class="slang-term">${item.term}</div>
                            <div class="slang-pinyin">${item.pinyin}</div>
                        </div>
                        <span class="tier-badge ${tierClass}">${item.tier.toUpperCase()}-TIER</span>
                    </div>
                    <span class="origin-tag">${item.origin}</span>
                    <div class="slang-meaning">${item.meaning}</div>
                    <div style="font-size: 0.85rem; color: var(--text-secondary); margin-bottom: 0.5rem;">
                        💡 ${item.desc}
                    </div>
                    <div class="example-box">
                        <div class="example-cn">${item.example}</div>
                        <div class="example-ru">${item.translation}</div>
                    </div>
                </div>
            `;
        }

        // Navigation
        function showSection(sectionId) {
            // Hide all sections
            document.querySelectorAll('section').forEach(s => s.classList.remove('active'));
            
            // Show target
            const target = document.getElementById(sectionId);
            if (target) {
                target.classList.add('active');
                target.scrollIntoView({ behavior: 'smooth', block: 'start' });
            }
            
            // Update tabs
            document.querySelectorAll('.nav-tab').forEach(t => t.classList.remove('active'));
            const tabs = ['heroSection', 'basics', 'moba', 'shooters', 'abbreviations', 'memes', 'confusing', 'quiz', 'random'];
            const index = tabs.indexOf(sectionId);
            if (index > 0) {
                document.querySelectorAll('.nav-tab')[index - 1]?.classList.add('active');
            }
            
            // Trigger animations
            setTimeout(() => {
                target?.querySelectorAll('.fade-in').forEach((el, i) => {
                    setTimeout(() => el.classList.add('visible'), i * 100);
                });
            }, 100);
        }

        // Quiz
        function initQuiz() {
            currentQuiz = 0;
            quizScore = 0;
            document.getElementById('quizResult').classList.remove('show');
            showQuizQuestion();
        }

        function showQuizQuestion() {
            const q = quizQuestions[currentQuiz];
            const progress = ((currentQuiz) / quizQuestions.length) * 100;
            document.getElementById('quizProgress').style.width = progress + '%';
            
            document.getElementById('quizContent').innerHTML = `
                <div class="quiz-question">
                    <div class="quiz-term">${q.term}</div>
                    <h3 style="color: var(--text-secondary); margin-bottom: 1.5rem;">${q.question}</h3>
                    <div class="quiz-options">
                        ${q.options.map((opt, i) => `
                            <div class="quiz-option" onclick="checkAnswer(${i})">${opt}</div>
                        `).join('')}
                    </div>
                </div>
            `;
        }

        function checkAnswer(selected) {
            const q = quizQuestions[currentQuiz];
            const options = document.querySelectorAll('.quiz-option');
            
            options.forEach((opt, i) => {
                opt.style.pointerEvents = 'none';
                if (i === q.correct) {
                    opt.classList.add('correct');
                } else if (i === selected && i !== q.correct) {
                    opt.classList.add('wrong');
                }
            });
            
            if (selected === q.correct) quizScore++;
            
            setTimeout(() => {
                currentQuiz++;
                if (currentQuiz < quizQuestions.length) {
                    showQuizQuestion();
                } else {
                    showQuizResult();
                }
            }, 1500);
        }

        function showQuizResult() {
            document.getElementById('quizContent').innerHTML = '';
            const result = document.getElementById('quizResult');
            result.classList.add('show');
            
            const rank = quizScore === 5 ? '🏆' : quizScore >= 3 ? '🥈' : '🥉';
            const title = quizScore === 5 ? '真大神！' : quizScore >= 3 ? '半熟玩家' : '还需练习';
            const text = quizScore === 5 
                ? 'Идеально! Ты разбираешься в китайском геймерском сленге лучше, чем многие носители!'
                : `Правильно ${quizScore} из ${quizQuestions.length}. Продолжай учить слэнг!`;
            
            document.getElementById('resultRank').textContent = rank;
            document.getElementById('resultTitle').textContent = title;
            document.getElementById('resultText').textContent = text;
        }

        function restartQuiz() {
            initQuiz();
        }

        // Random Meme Generator
        function generateRandomMeme() {
            const display = document.getElementById('memeDisplay');
            const random = allTerms[Math.floor(Math.random() * allTerms.length)];
            
            display.classList.add('active');
            display.innerHTML = `
                <div class="meme-term">${random.term}</div>
                <div class="meme-pinyin">${random.pinyin}</div>
                <div class="meme-meaning">${random.meaning}</div>
                <span class="meme-origin">${random.origin}</span>
                <div style="background: rgba(0,240,255,0.1); padding: 1rem; border-radius: 12px; margin-top: 1rem; max-width: 400px;">
                    <div style="font-family: 'Noto Sans SC'; font-size: 1.2rem; margin-bottom: 0.5rem;">${random.example}</div>
                    <div style="color: var(--text-secondary); font-size: 0.9rem;">${random.translation}</div>
                </div>
                <div style="margin-top: 1rem; font-size: 0.85rem; color: var(--text-secondary);">
                    💡 ${random.desc}
                </div>
            `;
            
            display.style.transform = 'scale(0.95)';
            setTimeout(() => display.style.transform = 'scale(1)', 100);
        }
    </script>
</body>
</html>
